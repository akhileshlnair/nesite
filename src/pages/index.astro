<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nair Engineering</title>
    
    <style>
        :root {
            /* --- RETRO-FUTURIST PALETTE (Extracted from your Image) --- */
            --void-dark: #0f1110;       /* Deepest organic black */
            --void-panel: #161816;      /* Slightly lighter panel bg */
            
            /* The "City Lights" Accents */
            --neon-cyan: #70f7ef;       /* The water/glass from the image */
            --neon-clay: #ff7e5f;       /* The terracotta roofs from the image */
            --neon-acid: #d4ff5f;       /* A high-contrast safety yellow/green */

            --text-bone: #e3e3dd;       /* Retro computer terminal white */
            --text-muted: #7a7d7a;      /* Faded technical text */
            
            /* --- BRUTALIST GRID VARS --- */
            --grid-line: rgba(112, 247, 239, 0.15);
            --glass-panel: rgba(22, 24, 22, 0.7);
            --border-tech: 1px solid var(--grid-line);
            
            --font-display: 'Helvetica Now Display', 'Inter', -apple-system, sans-serif;
            --font-mono: 'SF Mono', 'Space Mono', 'Courier New', monospace;
        }

        * { box-sizing: border-box; }

        body {
            background-color: var(--void-dark);
            color: var(--text-bone);
            margin: 0;
            font-family: var(--font-display);
            overflow-x: hidden;
            /* The subtle "film grain" texture overlay */
            background-image: 
                linear-gradient(rgba(15, 17, 16, 0.9), rgba(15, 17, 16, 0.9)),
                url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E");
        }

        /* --- GLOBAL LAYOUT GRID --- */
        .grid-overlay {
            position: fixed;
            top: 0; left: 0; right: 0; bottom: 0;
            z-index: 0;
            pointer-events: none;
            background-size: 40px 40px;
            background-image: 
                linear-gradient(to right, rgba(112, 247, 239, 0.03) 1px, transparent 1px),
                linear-gradient(to bottom, rgba(112, 247, 239, 0.03) 1px, transparent 1px);
            mask-image: radial-gradient(circle at center, black 40%, transparent 100%);
        }

        main {
            position: relative;
            z-index: 2;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
            border-left: var(--border-tech);
            border-right: var(--border-tech);
            background: linear-gradient(90deg, rgba(15,17,16,0.5), transparent 20%, transparent 80%, rgba(15,17,16,0.5));
            min-height: 100vh;
        }

        /* --- TYPOGRAPHY --- */
        h1 {
            font-size: clamp(4rem, 10vw, 8rem);
            font-weight: 800;
            line-height: 0.85;
            letter-spacing: -0.04em;
            margin: 0;
            text-transform: uppercase;
            /* Gradient text effect based on the image's "sunrise" */
            background: linear-gradient(180deg, var(--text-bone) 0%, var(--text-muted) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        h2 {
            font-size: 2.5rem;
            letter-spacing: -0.02em;
            margin-bottom: 1rem;
        }

        .mono-tag {
            font-family: var(--font-mono);
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            color: var(--neon-cyan);
            margin-bottom: 1rem;
            display: block;
        }
        
        .mono-tag.alert { color: var(--neon-clay); }

        p {
            font-size: 1.1rem;
            line-height: 1.6;
            color: var(--text-muted);
            max-width: 500px;
        }

        /* --- HEADER --- */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 2rem 0;
            border-bottom: var(--border-tech);
        }
        .brand {
            font-weight: 700;
            letter-spacing: -0.02em;
            font-size: 1.2rem;
            display: flex;
            gap: 10px;
            align-items: center;
        }
        .brand::before {
            content: '';
            width: 12px;
            height: 12px;
            background: var(--neon-clay);
            box-shadow: 0 0 10px var(--neon-clay);
        }

        /* --- HERO SECTION --- */
        .hero {
            padding: 8rem 0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            border-bottom: var(--border-tech);
        }

        .hero-visual {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* The Radial Halo from the image */
        .sun-halo {
            position: absolute;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(255, 126, 95, 0.15) 0%, rgba(0,0,0,0) 60%);
            z-index: -1;
            filter: blur(40px);
        }

        /* --- INPUT ISLAND (Retrofuturist Version) --- */
        .hud-input-group {
            margin-top: 3rem;
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            display: flex;
            padding: 4px;
            max-width: 420px;
        }
        
        .hud-input {
            background: transparent;
            border: none;
            color: var(--text-bone);
            padding: 1rem;
            flex: 1;
            font-family: var(--font-mono);
            font-size: 0.9rem;
            outline: none;
        }
        
        .hud-btn {
            background: var(--neon-cyan);
            color: var(--void-dark);
            border: none;
            padding: 0 1.5rem;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 0.05em;
            cursor: pointer;
            transition: all 0.2s;
        }
        
        .hud-btn:hover {
            background: var(--text-bone);
            box-shadow: 0 0 15px var(--text-bone);
        }

        /* --- SHOWCASE GRID --- */
        .showcase-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            border-bottom: var(--border-tech);
        }

        .showcase-content {
            padding: 6rem 4rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .showcase-visual {
            border-left: var(--border-tech);
            padding: 4rem;
            display: flex;
            justify-content: center;
            align-items: center;
            background: radial-gradient(circle at center, rgba(112, 247, 239, 0.05) 0%, transparent 70%);
            position: relative;
            overflow: hidden;
        }

        /* Phone Styling */
        .device-frame {
            position: relative;
            z-index: 10;
            width: 100%;
            max-width: 320px;
            transition: transform 0.5s cubic-bezier(0.2, 0.8, 0.2, 1);
        }
        
        .device-frame img {
            width: 100%;
            height: auto;
            display: block;
            filter: contrast(1.1) drop-shadow(0 20px 40px rgba(0,0,0,0.6));
        }

        .device-frame:hover {
            transform: scale(1.02) translateY(-10px);
        }

        /* --- DATA TABLE (Brutalist style) --- */
        .spec-table {
            width: 100%;
            margin-top: 2rem;
            border-top: 1px solid rgba(255,255,255,0.1);
        }
        .spec-row {
            display: flex;
            justify-content: space-between;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            font-family: var(--font-mono);
            font-size: 0.85rem;
        }
        .spec-val { color: var(--neon-cyan); }
        .spec-val.secure { color: var(--neon-clay); }

        /* --- FOOTER --- */
        footer {
            padding: 4rem 0;
            text-align: center;
            font-family: var(--font-mono);
            font-size: 0.7rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.2em;
        }

        /* --- UTILS --- */
        .text-glow-cyan { text-shadow: 0 0 20px rgba(112, 247, 239, 0.4); }
        .text-glow-clay { text-shadow: 0 0 20px rgba(255, 126, 95, 0.4); }

        @media (max-width: 900px) {
            .hero, .showcase-row { grid-template-columns: 1fr; }
            .showcase-visual { border-left: none; border-top: var(--border-tech); order: -1; }
            h1 { font-size: 15vw; }
        }
    </style>
</head>
<body>

    <div class="grid-overlay"></div>

    <main>
        <header>
            <div class="brand">NAIR ENGINEERING</div>
        </header>

        <section class="hero">
            <div>
                <span class="mono-tag">VIVRAL</span>
                <h1>Accessible<br>Medical<br><span style="color: var(--neon-cyan);" class="text-glow-cyan">Intelligence.</span></h1>

            </div>
            
            <div class="hero-visual">
                <div class="sun-halo"></div>
                <div style="width: 300px; height: 300px; border: 1px solid var(--neon-cyan); border-radius: 50%; display: flex; align-items: center; justify-content: center; position: relative;">
                    <div style="position: absolute; width: 100%; height: 1px; background: var(--neon-cyan);"></div>
                    <div style="position: absolute; height: 100%; width: 1px; background: var(--neon-cyan);"></div>
                    <div style="width: 200px; height: 200px; border: 1px solid var(--neon-clay); border-radius: 50%;"></div>
                </div>
            </div>
        </section>

        <section class="showcase-row">
            <div class="showcase-visual">
                <div class="device-frame">
                    <img src="/IMG_1656-portrait.png" alt="Vivral Interface">
                </div>
            </div>
            <div class="showcase-content">
                <span class="mono-tag">01 // REASONING ENGINE</span>
                <h2>The World's First<br>On-Device Medical AI.</h2>
                <p>
                    Powered by our state-of-the-art reasoning model. Vivral doesn't just guess; it thinks. 
                    Processing complex diagnostics with zero latency, entirely on your local hardware. Vivral will assist you from anywhere in the universe, no internet required. 
                </p>

                <div class="spec-table">
                    <div class="spec-row">
                        <span>ARCHITECTURE</span>
                        <span class="spec-val">NE.01R</span>
                    </div>
                    <div class="spec-row">
                        <span>LATENCY</span>
                        <span class="spec-val"0ms</span>
                    </div>
                </div>
            </div>
        </section>

        <section class="showcase-row">
            <div class="showcase-content">
                <span class="mono-tag alert">02 // PRIVACY</span>
                <h2 style="color: var(--neon-clay);" class="text-glow-clay">Patient Passport.</h2>
                <p>
                    Your data is yours. The NE Medical Passport stays encrypted in your phone's secure enclave. 
                    No information from Vivral ever touches the internet: completely HIPPA compliant. 
                </p>

                <div class="spec-table">
                    <div class="spec-row">
                        <span>ENCRYPTION</span>
                        <span class="spec-val secure">LOCAL</span>
                    </div>
                    <div class="spec-row">
                        <span>CLOUD UPLOAD</span>
                        <span class="spec-val secure">NONE</span>
                    </div>
                </div>
            </div>
            <div class="showcase-visual" style="border-left: var(--border-tech);">
                <div class="device-frame">
                    <img src="/IMG_1657-left.png" alt="Patient Passport Interface">
                </div>
            </div>
        </section>

        <section class="showcase-row">
            <div class="showcase-content">
                <span class="mono-tag alert">03 // MISSION STATEMENT</span>
                <h2 style="color: var(--neon-cyan);" class="text-glow-clay">Our Mission.</h2>
                <p>
                   We build advanced medical reasoning systems that deliver high-quality, trustworthy medical guidance to every person on Earth. We believe world-class healthcare should be accessible to everyone: at minimal cost and without barriers.
                </p>
            </div>
            <div class="showcase-visual" style="border-left: var(--border-tech);">
                <div class="device-frame">
                    <img src="/IMG_1659.jpeg" alt="Patient Passport Interface">
                </div>
            </div>
        </section>

        <footer>
            The Nair Engineering Company &copy; 2025 <br>
            Building the Future of Medicine
        </footer>

    </main>

</body>
</html>
